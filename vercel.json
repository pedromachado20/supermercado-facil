{
  "buildCommand": "bun run build",
  "installCommand": "bun install",
  "outputDirectory": "dist/client",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/server" }
  ],
  "crons": [
    { "path": "/api/cron/sweep", "schedule": "0 12 * * *" }
  ]
}
